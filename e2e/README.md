# Instrucciones para ejecutar los tests

1. Inicializar el backend con el comando `npm run start:test`

2. Inicializar el frontend con el comando `npm run dev`

3. Ejecutar los tests de playwright mediante el comando `npm run test`
